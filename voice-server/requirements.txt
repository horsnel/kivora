# =============================================================================
# Kivora Voice Server - Python Dependencies (ALL packages)
# =============================================================================
# For lightweight install (free tiers), use only the [core] section below.
# Full install includes all packages for all 10 services.
# =============================================================================

# ── Core (always needed, lightweight) ──
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
python-dotenv>=1.0.1
httpx>=0.27.0
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.14.0
pydub>=0.25.1

# ── TTS: Edge (cloud, free, fast — always available) ──
edge-tts>=6.1.0

# ── ASR: WhisperX / faster-whisper (99 languages, CPU-friendly) ──
faster-whisper>=1.1.0

# ── Translation: Argos (offline, free, 30+ languages) ──
argostranslate>=1.9.0

# ── Heavy ML packages (need GPU for production, optional for CPU) ──
# Uncomment these for full deployment with all 10 services:
# torch>=2.1.0
# torchaudio>=2.1.0
# TTS>=0.22.0
# audioseal
# demucs
# pyannote.audio
