# =============================================================================
# Kivora Voice Server - Python Dependencies
# =============================================================================
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
python-dotenv>=1.0.1

# TTS - Coqui XTTS (primary engine, 646 languages + voice cloning)
TTS>=0.22.0

# ASR - WhisperX / faster-whisper (fast, aligned transcription)
faster-whisper>=1.1.0

# Translation - Argos Translate (offline, free)
argostranslate>=1.9.0

# Audio processing
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.14.0

# Audio format conversion
pydub>=0.25.1

# HTTP client (for fallback translation APIs)
httpx>=0.27.0
